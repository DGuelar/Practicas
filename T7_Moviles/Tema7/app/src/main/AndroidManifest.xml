<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permiso necesario para mostrar notificaciones en Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PreferenciasDialogosNotificaciones">

        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 7.1 Preferencias -->
        <activity android:name=".MainActividad1"          android:exported="false" />
        <activity android:name=".PreferenciasAppActivity" android:label="PREFERENCIAS" />

        <!-- 7.2 Toast -->
        <activity android:name=".MainActividad2" android:exported="false" />
        <!-- 7.3 Diálogos -->
        <activity android:name=".MainActividad3" android:exported="false" />
        <!-- 7.4 DatePicker y TimePicker -->
        <activity android:name=".MainActividad4" android:exported="false" />
        <!-- 7.5 Notificaciones -->
        <activity android:name=".MainActividad5" android:exported="false" />
    </application>

</manifest>