<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidAvanzado">

        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActividad93"  android:exported="false" />
        <activity android:name=".FragmentosActivity" android:exported="false" />
        <activity android:name=".MainActividad95"  android:exported="false" />
        <activity android:name=".MainActividad96"  android:exported="false" />
        <activity android:name=".MainActividad98"  android:exported="false" />
        <service android:name=".CronometroService" />
        <activity android:name=".MainActividad910" android:exported="false" />
    </application>

</manifest>