<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Persistencia">

        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActividad102" android:exported="false" />
        <activity android:name=".MainActividad104" android:exported="false" />
        <activity android:name=".MainActividad105" android:exported="false" />
        <activity android:name=".MainActividad108" android:exported="false" />
        <activity android:name=".MainActividad109" android:exported="false" />
        <provider
            android:name=".VersionesProvider"
            android:authorities="com.guelar.persistencia.provider"
            android:exported="false" />

    </application>

</manifest>